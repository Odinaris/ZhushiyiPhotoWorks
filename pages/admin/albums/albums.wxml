<view class="admin-page">
  <view class="page-header">
    <view class="header-title">作品集管理</view>
    <button class="add-btn" bindtap="addAlbum">+ 添加</button>
  </view>

  <view class="album-list" wx:if="{{albums.length > 0}}">
    <view class="album-card" wx:for="{{albums}}" wx:key="_id">
      <image class="album-cover" src="{{item.coverImage}}" mode="aspectFill"></image>
      <view class="album-info">
        <view class="album-title">{{item.title}}</view>
        <view class="album-meta">
          <text>{{item.categoryName}}</text>
          <text class="divider">·</text>
          <text>{{item.images.length}}张</text>
          <text class="divider">·</text>
          <text class="{{item.isActive ? 'active' : 'inactive'}}">{{item.isActive ? '已启用' : '已禁用'}}</text>
          <text class="divider">·</text>
          <text class="{{item.isFeatured ? 'featured' : ''}}">{{item.isFeatured ? '精选' : '普通'}}</text>
        </view>
      </view>
      <view class="album-actions">
        <button class="action-btn edit" bindtap="editAlbum" data-id="{{item._id}}">编辑</button>
        <button class="action-btn delete" bindtap="deleteAlbum" data-id="{{item._id}}">删除</button>
      </view>
    </view>
  </view>

  <view class="empty-container" wx:else>
    <view class="empty-icon">📸</view>
    <view class="empty-text">暂无作品集</view>
  </view>
</view>
